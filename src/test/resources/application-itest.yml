# property needed when running the integration tests for embedded mongodb.
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 5.0.5

spring:
  data:
    mongodb:
      host: localhost
      database: saintseiyauniverse-itest
